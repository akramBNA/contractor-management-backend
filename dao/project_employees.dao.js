const { project_employees } = require("../models/project_employees.models");

class project_employeesDao {
    async getProjectAssignedEmployees(req, res, next) {
        try {

        } catch (error) {
            return next(error);
        }
    }
}

module.exports = project_employeesDao;
